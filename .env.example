# Required after the PostgreSQL migration phase.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/dms_demo"

# Generate a unique value per environment; never commit a real secret.
SESSION_SECRET="replace-with-a-long-random-value"
